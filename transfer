# Vérifier doublons par année et département
print(df_ti_dept_work.groupby(['Year', 'num_dpt']).size().reset_index(name='counts').query('counts > 1'))

# Population totale par année après agrégation
pop_par_annee = df_ti_dept_work.groupby('Year')['population'].sum()
print(pop_par_annee)
