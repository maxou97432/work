je veux que tu fasses la répartitions des na: tu comptes le nombre d'occurence de chaque porcentage et que tu traces leurs répartitions: df_filtre_prix_na = df_filtre["Prix_m2"].isna().sum()
print(f"{df_filtre_prix_na} données manquantes")

na_par_ville = (df_filtre.groupby("ville_")["Prix_m2"].apply(lambda x: x.isna().mean()*100).sort_values(ascending = False))
print(na_par_ville)
ax = na_par_ville.plot(kind = "bar", figsize = (12,5))
ax.set_xticklabels([])
ax.set_ylabel("% NA")
ax.set_xlabel("")
